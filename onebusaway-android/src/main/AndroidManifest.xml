<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2011-2022 Paul Watts (paulcwatts@gmail.com),
     University of South Florida (sjbarbeau@gmail.com),
     Microsoft Corporation.

     Licensed under the Apache License, Version 2.0 (the "License");
     you may not use this file except in compliance with the License.
     You may obtain a copy of the License at

          http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
     See the License for the specific language governing permissions and
     limitations under the License.
-->
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="${applicationId}.permission.TRIP_SERVICE"
        android:label="@string/trip_service_perm_label"
        android:description="@string/trip_service_perm_description"
        android:protectionLevel="normal"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />
    <uses-permission android:name="${applicationId}.permission.TRIP_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Permission request for activity recognition and background location is disabled because the related feature has been turned off.
    See issue #1240 for more details: https://github.com/OneBusAway/onebusaway-android/issues/1240-->
    <!-- ACTIVITY_RECOGNITION API 28 and lower -->
<!--    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />-->
<!--    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />-->
    <!-- ACTIVITY_RECOGNITION API 29 and higher -->
<!--    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />-->
    <!--To enable checkBatteryOptimizations feature, also uncomment checkBatteryOptimizations() in
    the HomeActivity's onCreate() method-->
    <!--<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>-->
    <!-- For reminders - SCHEDULE_EXACT_ALARM is required when targeting Android 12 or higher-->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- For notifications, POST_NOTIFICATIONS is required when targeting Android 12 or higher-->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="false"
        android:name="org.onebusaway.android.app.Application"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.OneBusAway.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config">


        <meta-data
            android:name="android.app.default_searchable"
            android:value="org.onebusaway.android.ui.HomeActivity" />

        <!-- Single-activity: HomeActivity is the launcher AND the system-search target
             (ACTION_SEARCH); its translator routes the query to the search NavHost destination. -->
        <activity
            android:name="org.onebusaway.android.ui.HomeActivity"
            android:launchMode="singleTop"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <!-- The `onebusaway://add-region` deep link (former SettingsActivity VIEW
                 filter). HomeActivity.applyIntentSideEffects reads the oba-url/otp-url query params,
                 applies the custom API URL(s), and stays on the home/map path (IntentRouteMapper). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="add-region"
                    android:scheme="onebusaway" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
            <!-- Static app shortcuts (long-press the launcher icon) for the My* list
                 screens, replacing the legacy launcher CREATE_SHORTCUT picker. Each opens HomeActivity
                 with a NAV_ROUTE extra the translator navigates to. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <!-- Arrivals is a HomeActivity NavHost destination. These frozen component names
             (old pinned launcher shortcuts) keep resolving via aliases → HomeActivity, which reads the
             stop data URI and navigates. Aliases inherit launchMode/configChanges from HomeActivity. -->
        <activity-alias
            android:name="org.onebusaway.android.ui.ArrivalsListActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="com.joulespersecond.seattlebusbot.ArrivalsListActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="org.onebusaway.android.ui.StopInfoActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity-alias>
        <!-- Route info is a HomeActivity NavHost destination; frozen route-shortcut
             component names resolve via aliases → HomeActivity (reads the route data URI, navigates). -->
        <activity-alias
            android:name="org.onebusaway.android.ui.RouteInfoActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="com.joulespersecond.seattlebusbot.RouteInfoActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity-alias>
        <!-- Trip details is a HomeActivity NavHost destination; the frozen name resolves
             via an alias → HomeActivity (cheap insurance for an in-flight NavigationService PendingIntent). -->
        <activity-alias
            android:name="org.onebusaway.android.ui.TripDetailsActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity-alias>

        <!-- The My* list screens are HomeActivity NavHost destinations now. These frozen
             component names (old pinned launcher shortcuts / CREATE_SHORTCUT pickers) keep resolving via
             aliases → HomeActivity, whose translator reads the tab:// data URI and navigates to the
             matching My* destination. The legacy in-Activity CREATE_SHORTCUT *picker* is dropped (it's
             replaced by static app shortcuts + the in-app pin actions), so a fresh CREATE_SHORTCUT
             request now lands on the list screen rather than a row-picker. -->
        <activity-alias
            android:name="org.onebusaway.android.ui.MyStopsActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/stop_shortcut"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="com.joulespersecond.seattlebusbot.MyStopsActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/stop_shortcut"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="org.onebusaway.android.ui.MyRecentStopsActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/recent_stops_shortcut"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="org.onebusaway.android.ui.MyStarredStopsActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/starred_stops_shortcut"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="org.onebusaway.android.ui.MyRoutesActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/route_shortcut"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="com.joulespersecond.seattlebusbot.MyRoutesActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/route_shortcut"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name="org.onebusaway.android.ui.MyRecentRoutesActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/recent_routes_shortcut"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity-alias>
        <!-- The report flow (ReportActivity / CustomerServiceActivity /
             InfrastructureIssueActivity) is now HomeActivity NavHost destinations (REPORT /
             CUSTOMER_SERVICE / INFRASTRUCTURE_ISSUE). The former classes are launcher facade objects
             that build HomeActivity nav intents; no <activity> entries are needed (all were
             non-exported with no intent-filters / aliases). -->

        <!-- The "Recent" screen is a HomeActivity NavHost destination (MY_RECENT). This
             frozen name resolves via an alias → HomeActivity. A legacy self-shortcut pinned with no
             data URI (the dropped CREATE_SHORTCUT self-pin branch) carries no tab://, so it falls
             through the translator to the home/map path — an accepted rare legacy case. -->
        <activity-alias
            android:name="org.onebusaway.android.ui.MyRecentStopsAndRoutesActivity"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:label="@string/my_recent_title"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity-alias>
        <!-- Night light is a HomeActivity NavHost destination. The frozen component name
             (old pinned launcher shortcuts) resolves via this alias → HomeActivity, which routes to
             the night-light destination by component name (the intent carries no data URI). -->
        <activity-alias
            android:name="org.onebusaway.android.ui.NightLightActivity"
            android:label="@string/stop_info_option_night_light"
            android:targetActivity="org.onebusaway.android.ui.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity-alias>


        <!-- Provider is defined in build.gradle flavors -->
        <provider
            android:name="org.onebusaway.android.provider.ObaProvider"
            android:authorities="${databaseAuthority}"/>

        <!-- Shares captured/picked report photos with the camera and Open311 endpoints -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <service
            android:name=".directions.realtime.RealtimeService">
        </service>

        <service
            android:name=".nav.NavigationService"
            android:foregroundServiceType="location" />

        <receiver
            android:name=".nav.NavigationReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action android:name=".nav.NavigationReceiver" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".nav.FeedbackReceiver"
            android:enabled="true">
        </receiver>

        <receiver android:name=".directions.realtime.RealtimeWakefulReceiver"
            android:exported="true">
            <intent-filter>
                <!-- Should match constants for actions defined in OTPConstants -->
                <action android:name="${applicationId}.directions.action.START_CHECKS"/>
                <action android:name="${applicationId}.directions.action.CHECK"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/apiv2_key"/>


        <!-- Opt-out of June 2018 Android Maps styling with pin POI icons - see #891 -->
        <meta-data android:name="com.google.android.gms.maps.API_OPTIONS" android:value="B3H9HE845CFHYG"/>
        <meta-data
                android:name="com.google.firebase.messaging.default_notification_icon"
                android:resource="@drawable/ic_stat_notification" />
        <meta-data
                android:name="com.google.firebase.messaging.default_notification_color"
                android:resource="@color/theme_primary" />
        <service
                android:name=".tripservice.MyFirebaseMessagingService"
                android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>
</manifest>